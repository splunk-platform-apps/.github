name: Auto Assign Issues
on:
  issues:
    types: [opened]

permissions:
  issues: write # Grant write permission to issues

jobs:
  auto-assign:
    runs-on: ubuntu-latest
    steps:
      - name: "Auto-assign issue"
        uses: pozil/auto-assign-issue@v2
        with:
          assignees: shalper2,joseacunacarrera,edro15
          # teams: admins
          numOfAssignee: 1
          allowSelfAssign: false
          # Auto-assigns new issues to one user randomly chosen from teams or assignees.
          # It won't self-assign to the issue author
          # TODO evaluate and eventually assign to team! - requires PAT / Github App configuration.
          # https://github.com/pozil/auto-assign-issue